node_modules/
data/
*.log

# config.json SÍ va al repo: así clonás y arranca configurado.
# Ojo: tiene el número de la dueña. Mantené el repo PRIVADO.

# Lo propio de CADA instalación va acá: números, nombre del negocio, precios
# del cliente. Al no estar versionado, "git pull" nunca lo pisa ni da conflicto.
config.local.json
